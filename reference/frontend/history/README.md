# History - Visual Reference

Reference images for the historical batches view (past runs, filtering, pagination).

Add renders here when produced (do not fabricate):

```
history-light.png
history-dark.png
```

Until then, follow the Recent Batches table pattern in the dashboard reference and
`docs/04-frontend/components.md` (Tables).
