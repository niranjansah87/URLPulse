# Batch Detail — Visual Reference

Reference images for a single batch: progress, URL results table, cancel / retry-failed,
download.

Add renders here when produced (do not fabricate):

```
batch-detail-light.png
batch-detail-dark.png
```

Until then, follow `docs/04-frontend/components.md` (Progress, URL result, Tables),
`responsive-design.md`, and `empty-loading-error-states.md`.
