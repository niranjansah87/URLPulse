# Alerts — Visual Reference

Reference images for the Alerts view (notifications / alert configuration). Future scope —
not part of the current product milestones; documented here so the nav item has a home.

Add renders here when produced (do not fabricate):

```
alerts-light.png
alerts-dark.png
```
