# Create Batch - Visual Reference

Reference images for the batch-creation flow (manual URL entry + CSV upload).

Add renders here when produced (do not fabricate):

```
create-batch-light.png
create-batch-dark.png
```

Until then, follow `docs/04-frontend/components.md` (Dialogs / forms),
`empty-loading-error-states.md` (validation), and `accessibility.md` (labels, errors).
